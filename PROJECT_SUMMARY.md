# 필람(Pillarm) — 프로젝트 진행 이력 요약

> 이력서·포트폴리오 작성용 요약 문서. 실제 커밋 이력(`git log`)과 PRD 문서를 근거로 작성함.

## 한 줄 소개

약 복용 시간을 등록하면 알림을 주고 복용 여부를 기록·통계로 보여주는 **오프라인 우선(offline-first) 모바일 앱**. 소셜 로그인, 보호자 공유, AI 기반 약봉투 자동 인식까지 기획부터 서버 인프라·스토어 배포까지 단독으로 수행한 개인 프로젝트.

| 항목 | 내용 |
|---|---|
| 기간 | 2026-04-22 ~ 진행 중 (약 4개월) |
| 형태 | 1인 개발 — Claude Code(AI 코딩 에이전트)와 페어 프로그래밍 |
| 커밋 수 | 104개 (2026-08-21 기준) |
| 플랫폼 | iOS / Android — React Native (Expo) + TypeScript |
| 서버 | Node.js/Express + Prisma + PostgreSQL(Supabase), Railway 배포 |
| 배포 현황 | **App Store 심사 승인·정식 배포 완료** (iOS build 27), Android는 Play Store 제출 전 단계 |

---

## 핵심 기능

- **일정 관리**: 복용 일정 등록·수정·전체삭제, 로컬 알림(expo-notifications) 스케줄링/재등록
- **복용 기록·통계**: 탭 1회 복용 체크, 주간/월간 완료율 통계
- **보호자 공유(Care Circle)**: 보호자 초대(QR/링크), 피보호자 복용 현황 실시간 모니터링
- **소셜 로그인**: Apple / Google / Kakao 3종 (네이버는 도입 후 효용 대비 복잡도 문제로 제거)
- **AI 약봉투 스캔**: 카메라로 약봉투를 찍으면 Claude Vision(Haiku 우선, 실패 시 Sonnet 폴백)이 약 이름·용량·복용법을 인식해 일정 초안 자동 생성, 포(packet) 단위 그룹화
- **영양제 복용 가이드**: 카테고리·복용 시기·출처 포함 오프라인 가이드
- **관리자 패널**: 유저 통계, 전체 푸시 발송, 기능 플래그 on/off
- **오프라인 우선**: 모든 데이터를 SQLite에 우선 저장, 재연결 시 서버와 자동 동기화

---

## 기술적으로 의미 있었던 작업

실제로 겪은 버그·이슈를 원인 분석해 해결한 사례 위주로 정리 (문제 → 원인 → 해결).

1. **AI 인식 결과 응답 잘림(토큰 제한) 진단·해결**
   다수(8개 이상) 약물이 담긴 복약안내문 이미지에서 스캔이 계속 실패하는 버그를 서버 로직 분석으로 추적 — Claude API 응답의 `max_tokens` 상한(1024)에 걸려 JSON이 중간에 잘리는 것이 원인이었음을 특정하고 상향 조정(4096)으로 해결.

2. **동시성(race condition) 버그 수정**
   약봉투 스캔 일일 사용 횟수 제한이 동시 요청 시 우회 가능했던 문제를 "조회 후 검사" 방식에서 **원자적 upsert 기반 선점** 방식으로 교체해 해결.

3. **오프라인 동기화 버그 수정**
   오프라인 상태에서 기록한 복용 데이터가 재연결 후에도 서버에 반영되지 않던 문제의 근본 원인을 pending 큐 로직에서 찾아 수정.

4. **보안 강화 다수 진행**
   - 인증 토큰 저장소 AsyncStorage → SecureStore 전환
   - 서버 API rate limiting 추가
   - `/sync` 엔드포인트에 소유권 검증 추가 (타 유저 레코드 접근 방지, IDOR 방지)
   - 로그인 토큰에 `isAdmin` 누락 수정, 계정 삭제 로직 트랜잭션화
   - 대량 업로드(`/sync/push`) 소유권 검증 추가

5. **App Store 심사 대응 실무 경험**
   - 마이크 권한 사용 목적이 불명확하다는 사유로 반려 → 실제로 쓰지 않는 `NSMicrophoneUsageDescription`/`RECORD_AUDIO` 권한 자체를 제거해 재제출
   - Guideline 2.1(정보 부족) — 리뷰어가 실물 약봉투를 촬영할 수 없어 기능 검증이 막힌 상황에 대해, 다양한 케이스(단일 약/다중 약 조제표/실사 촬영 각도/저해상도 손글씨 메모)의 합성 테스트 이미지를 스크립트로 생성해 회신, 심사 재개

6. **UX 개선 — 원터치 확인 플로우 설계**
   AI가 인식한 정보를 8개 입력 필드가 펼쳐진 폼으로 보여주던 화면을, 기본은 요약 카드 한 장 + 등록 버튼 한 번으로 끝나고 필요할 때만 상세 폼을 펼치는 방식으로 재설계. 접근성 기준(최소 44×44pt 터치 영역, 16sp 이상 폰트) 적용.

7. **약 슬라이드 & 포(packet) 그룹화 UX**
   여러 약이 한 번에 인식됐을 때 탭 전환뿐 아니라 좌우 스와이프로 넘겨볼 수 있는 페이징 UI 구현. 같은 시간대 약들을 사용자가 직접 선택해 포로 묶고(2개 이상 필수) 개별 삭제할 수 있는 기능 설계.

---

## 배포·인프라

- **서버**: Railway에 Express 서버 배포, PostgreSQL은 Supabase(pgbouncer 커넥션 풀링) 사용, Prisma ORM
- **CI/배포 파이프라인**: EAS Build로 iOS/Android 네이티브 빌드, EAS Submit으로 App Store Connect 자동 업로드
- **iOS**: App Store 정식 심사 승인·배포 완료 (build 27, 2026-08-21) — 반려 대응 2회 경험(마이크 권한, Guideline 2.1) 포함
- **Android**: EAS 빌드까지 완료, Google Play Console 등록 및 정식 제출은 **다음 작업**

---

## 테스트

- Jest + React Native Testing Library
- 클라이언트 테스트 431개, 서버 테스트 70개 전부 통과 (2026-08-21 기준)

---

## 개발 단계(Phase) 개요

| Phase | 목표 | 상태 |
|---|---|---|
| 1 — MVP | 등록·알림·체크·통계 핵심 루프 | ✅ 완료 |
| 2 — 확장 | 보호자 공유·약 DB 연동·포인트·AI 코칭 | ✅ 완료 |
| 3 — 배포 | 소셜 로그인, 서버·스토어 배포, 오프라인 처리, 관리자 패널 | 🔧 iOS 완료 · Android 배포 남음 |
| 4 — 스캔 | 약봉투 촬영 → AI 자동 일정 생성, 영양제 가이드 | ✅ 완료 (지속 개선 중) |

각 Phase 상세 요구사항·완료 기준은 `PRD_PHASE1.md` ~ `PRD_PHASE4.md` 참고.

---

## 다음 단계

- **Google Play Console 등록 및 Android 정식 배포**
