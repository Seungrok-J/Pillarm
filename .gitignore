node_modules/
.expo/
dist/
web-build/
coverage/
android/
ios/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
google-play-service-account.json
*-service-account*.json
*.orig.*
web-build/
.env
.env.local
.env.*
server/.env
server/.env.local
server/.env.*
.claude/settings.local.json
.claude/scheduled_tasks.lock
server/supabase/.temp/

# IDE — Android Studio 가 열어본 하위 디렉터리마다 .idea 를 새로 만든다.
# 루트 .idea 의 프로젝트 설정은 추적하되, 기기별 캐시와 하위 사본은 무시한다.
src/.idea/
.idea/caches/

# EAS 빌드 산출물 — 수동 업로드용으로 내려받는 자리
build-artifacts/
