# 세션 기록 — 2026-08-25 (Android 배포 + 스캔/일정 기능 개선)

> 폴더명을 한글("필람(Pillarm)")에서 영문으로 바꾸는 작업 직전에 남긴 스냅샷.
> 폴더명 변경으로 Claude Code 세션이 끊길 수 있어서, 새 세션에서 이어갈 수 있도록 기록함.

## 1. 배포 현황

### iOS
- **1.0.1 (build 30)** App Store 심사 제출 완료 (2026-08-25) — 결과 대기 중
- build 27 → 30 변경사항: 카카오 로그인 scopes 파라미터 버그 수정, 앱 아이콘/스플래시 개선, 보호자 탭 갱신 버그 수정

### Android
- Google Play Console: Closed testing(Alpha) 트랙, versionCode 21 이미 제출해 Google 검토 통과 → 현재 살아있는 버전
- **versionCode 22**로 로컬에서 올려둔 상태 (아직 Play Console에 제출 안 함) — 이번 세션에서 만든 변경사항(카카오 버그 수정, UI 수정, 스캔 포 빌더 재설계, 드래그앤드랍)이 전부 포함됨
- `eas.json`의 `submit.production.android.track`을 `internal` → `alpha`로 변경해둠 (Closed testing 트랙 API명)
- Closed testing 요건: 테스터 12명 이상·14일 연속 참여 필요 (Internal testing과는 별개 트랙) — 아직 테스터 모집 전
- **최신 EAS 클라우드 preview 빌드**: https://expo.dev/accounts/seungrok-j/projects/pillarm/builds/4eba231a-c5b1-4dea-8c90-3a401249d2e9 (pack builder + drag smoothness 전부 포함, 설치해서 테스트 가능)

## 2. 이번 세션에 고친 버그

| 버그 | 원인 | 수정 |
|---|---|---|
| Android 카카오 로그인 — 카카오톡 미설치 시 "계속하기" 눌러도 안 넘어감 | `app.json`의 `@react-native-kakao/core` 플러그인 설정에 `android: { authCodeHandlerActivity: true }`가 빠져있어서, 웹 로그인 리다이렉트를 받는 `AuthCodeHandlerActivity`의 인텐트 필터가 매니페스트에 아예 등록 안 됨 | `app.json`에 추가 완료 |
| Android Google/Kakao 로그인 DEVELOPER_ERROR (직접 설치한 preview APK에서만) | Play App Signing 키(스토어용)와 EAS 업로드 키(직접 설치 APK용)의 SHA-1/키해시가 달라서, 업로드 키가 Google Cloud Console·카카오 디벨로퍼스에 등록 안 돼 있었음 | 업로드 키 SHA-1(`0F:C0:42:...`)과 카카오 키해시(`D8BCAtJ0lk6yc6NHEqEmdfd7vH8=`) 둘 다 등록 완료 |
| `ScheduleFormScreen`(일정 추가/수정) 저장 버튼이 하단 시스템 내비게이션 바에 가려짐 | `SafeAreaView` 자체가 없어서 Android edge-to-edge 렌더링 시 하드코딩 padding(20)만으론 부족 | `SafeAreaView edges={['bottom']}` 추가 |
| `ThemeShopScreen`(테마 상점) — **Android에서만** 안전영역 미적용 | `SafeAreaView`를 `react-native` 코어에서 import (iOS 전용 구현, Android에선 no-op) | `react-native-safe-area-context`로 교체 |
| `CareCircleScreen`, `CareMonitorScreen` | `edges={['top']}`만 있거나 SafeAreaView 자체가 없음 | `edges={['top','bottom']}` 추가/신규 적용 |

모든 안전영역 수정은 `react-native-safe-area-context`의 insets 기반이라 iOS/Android 둘 다 동일하게 대응됨.

## 3. 기능 변경

### 스캔 결과 화면(`ScanResultScreen.tsx`) — 포(packet) 빌더 재설계
- **요구사항**: 실제 약국 포장처럼, 같은 약이 시간대가 다른 여러 포에 겹쳐 들어갈 수 있어야 함
  - 예: 약1,3,5 → "아침·저녁 포", 약1,5 → "점심 포"(약3은 졸림 성분 때문에 점심 제외), 나머지는 개별
- 데이터 모델: `Pack { id, name, times: string[], memberIdxs: number[] }` — 여러 개의 `packs` 배열
- 최초 진입 시 완전히 동일한 시간 조합을 가진 약(2개 이상)은 자동으로 기본 포 생성, 이후 자유 편집 가능
- 시간은 체크박스로 선택(해당 시간을 다 가진 약만 후보로 표시), "+ 새 포 만들기"로 포 추가, 각 포마다 이름 입력 가능
- 같은 약이 여러 포에 속할 수 있되, **겹치는 시간을 두 포가 동시에 점유하는 건 UI 단에서 후보 필터링으로 차단**(모순 방지)
- 저장 시(`handleCreate`): 약 1개가 여러 포에 속하면 **포별로 별도의 Schedule row**를 생성(같은 medicationId, 다른 packetId), 포에 안 속한 나머지 시간은 개별 Schedule로 별도 생성

### 일정관리 화면(`ScheduleManageScreen.tsx`) — 드래그앤드랍으로 포 합치기
- `react-native-draggable-flatlist` + `react-native-reanimated`(v4) 도입
- 개별 일정을 길게 눌러 다른 개별/포 위로 드래그하면 병합 — **복용 시간과 기간(시작일·종료일)이 완전히 같을 때만** 허용, 다르면 Alert로 거부
- 병합 시 DB 업데이트(`upsertSchedule`) + 미래 DoseEvent 재생성(`deleteFutureDoseEvents` + `scheduleForSchedule`) + 서버 동기화(`pushSchedule`)
- 부드러운 UX: 드래그 중 카드 확대(reanimated spring 애니메이션, `DragScale` 컴포넌트) + 햅틱 피드백(`expo-haptics`) — 드래그 시작 시 Medium impact, 병합 성공 시 Success, 거부 시 Warning

### 새로 추가된 네이티브 의존성 (로컬 빌드 필요)
- `react-native-reanimated` ~4.1.1
- `react-native-draggable-flatlist` ^4.0.3
- `expo-haptics`
- `babel.config.js`에 `react-native-reanimated/plugin` 추가
- `App.tsx`에 `GestureHandlerRootView` 루트 래핑 추가

## 4. 로컬 빌드 이슈 (진행 중이던 작업)

EAS 클라우드 사용량을 아끼기 위해 로컬 Android 빌드(`npx expo run:android`)로 전환 시도 중 발견한 문제:

- 프로젝트 경로에 있는 **한글 폴더명("필람")**이 Windows 네이티브 빌드 툴체인에서 계속 문제를 일으킴
  1. NDK clang의 precompiled header(.pch) 처리가 유니코드 경로에서 깨짐 (`expo-modules-core`, `react-native-screens`, `react-native-worklets` 빌드 실패)
  2. `subst`로 가상 드라이브(P:) 매핑 시도 → Gradle/네이티브 빌드는 통과했지만, `expo-modules-autolinking` npm 패키지의 `findPackageJsonPathAsync` 함수가 드라이브 루트("P:\")를 다루는 로직에 버그가 있어서(`path.dirname('P:') === 'P:'`이라 루프가 안 돎) 여전히 실패
- **결론**: 근본 해결은 프로젝트 폴더명을 영문으로 바꾸는 것 (이전에 create-expo-app 최초 설치 때도 동일한 이슈로 임시 폴더 경유했던 전례 있음 — `project_pillarm_setup.md` 메모리 참고)
- 사용자 확인 하에 폴더명 변경 진행 예정: `C:\Users\jsl\Desktop\필람(Pillarm)` → 영문 경로
- **주의**: Claude Code 세션이 이 폴더를 기준 작업 디렉터리로 잡고 있어서, 이름을 바꾸면 이 세션의 파일 도구들이 끊길 수 있음 — 그래서 이 기록을 남김

## 5. 폴더명 변경 후 해야 할 일

1. 새 경로에서 Claude Code 세션 재시작 (또는 기존 세션이 계속 동작하는지 확인)
2. `npx expo run:android`로 로컬 빌드 재시도 — 이번엔 경로 문제 없이 성공해야 함
3. 로컬 빌드로 카카오/구글 로그인, 스캔 포 빌더(여러 포 겹침), 일정관리 드래그앤드랍을 실기기에서 테스트
4. 전부 확인되면 versionCode 22로 production 빌드 → Play Console Closed testing(Alpha) 트랙에 수동 업로드
5. Closed testing 테스터 12명 이상 모집, 14일 연속 참여 채우기
6. iOS 1.0.1 심사 결과 확인
