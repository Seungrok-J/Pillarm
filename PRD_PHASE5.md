# PRD Phase 5 — 수익 모델

> **전제 조건:** Phase 3 배포 완료 + **Android 프로덕션 액세스 확보 후** 진행한다.
> **목표:** 핵심 복약 기능은 영구 무료로 유지하면서, 보호자가 결제하는 프리미엄 구독으로 수익을 만든다.
>
> **진행 상태 (2026-09-02):** 기획 단계. 구현 착수 전.
> 착수 조건은 아래 [0. 착수 시점](#0-착수-시점--지금-시작하지-않는-이유)을 먼저 확인할 것.

---

## 0. 착수 시점 — 지금 시작하지 않는 이유

**2026-09-02 현재 구현에 착수하지 않는다.** 순서는 다음과 같다.

| 순서 | 조건 | 시점 |
|------|------|------|
| 1 | Android 프로덕션 액세스 확보 (테스터 12명 14일 연속) | 빠르면 2026-09-11 |
| 2 | 정식 출시 후 실사용 데이터 수집 (아래 지표) | 출시 후 4~6주 |
| 3 | 데이터로 가격·경계선 검증 후 결제 도입 | 그 다음 |

**Why:** 비공개 테스트로 프로덕션 액세스를 심사받는 중에 인앱결제를 추가하면 심사 항목이 늘고
반려 위험이 커진다. 또한 아래 지표를 모르는 상태에서 정한 가격·기능 경계선은 근거가 없다.

### 착수 전 확보해야 할 지표

| 지표 | 왜 필요한가 | 목표선 |
|------|------------|--------|
| **보호자 그룹 사용률** | 이 모델의 성패를 좌우하는 단일 지표 | 15% 이상 |
| 보호자 1명당 관리 환자 수 | "다중 관리" 유료화가 값을 갖는지 | 평균 1.3명 이상 |
| 스캔 실사용 빈도 | 무제한 스캔이 혜택이 되는지 | — (측정만) |
| 30일 리텐션 | 구독이 성립할 최소 조건 | 25% 이상 |

**보호자 그룹 사용률이 10% 미만이면 이 PRD의 전제를 재검토한다.** 그 경우 개인 대상
일회성 구매(평생 이용권 단독) 또는 B2B 선회를 검토한다.

---

## 1. 전략 요약

### 핵심 판단 — 돈 내는 사람 ≠ 쓰는 사람

복용자는 고령층이라 앱 결제 의향이 매우 낮다. 반면 **보호자(자녀, 40~50대)는 지불 의사와
동기를 모두 갖는다.** "부모님이 약을 안 드신다"는 불안이 결제 동기다.

따라서 **한 명이 결제하면 그룹 전체가 프리미엄이 되는** 구조로 간다.
보호자 그룹(`CareCircle`)은 이미 구현되어 있으므로 새로 만들 것이 아니라 과금 지점을 붙인다.

### 절대 유료화하지 않는 것 (영구 무료)

- 복용 일정 등록
- **알림**
- 복용 체크
- 기본 통계 (오늘/이번 주 복용률)

**약 알림을 결제 뒤에 두지 않는다.** 건강에 직접 해를 끼칠 수 있고 앱스토어 심사·평판
양쪽에서 위험하다. 이 원칙은 스토어 설명과 결제 화면에 명시해 마케팅 자산으로 쓴다.

### 현재 비용 구조 (2026-09-02 기준)

| 항목 | 성격 | 비고 |
|------|------|------|
| AI 스캔 (Claude Vision) | **유일한 변동비** | haiku 우선 → sonnet 폴백, 일 5회 제한 |
| AI 코칭 | 비용 0 | `generateCoachingMessages()` — 로컬 규칙 기반, 네트워크 호출 없음 |
| 서버 (Railway + DB) | 고정비 | |
| FCM | 사실상 0 | |

스캔은 처방받을 때만 쓰므로 실사용은 월 1~3회 수준으로 예상된다.
**변동비가 낮다는 것은 낮은 ARPU로도 사업이 성립한다는 뜻이지만, 동시에 "무제한 스캔"은
단독으로 값을 매길 만한 혜택이 못 된다는 뜻이다.** 유료 가치는 보호자 기능과 리포트에 둔다.

---

# Feature 1 — 프리미엄 구독

## 1. 기능 개요

### 요금제

| 플랜 | 가격 | 비고 |
|------|------|------|
| 무료 | ₩0 | 핵심 복약 기능 전부 |
| 월 구독 | ₩3,900 | |
| **연 구독** | **₩29,000** | 약 38% 할인 — 기본 유도 대상 |
| 평생 이용권 | ₩59,000 | 비소모성 1회 구매 — **초기 한정 판매** |

### 평생 이용권은 초기 한정으로만 판매한다

한국 중장년층은 구독을 심리적으로 강하게 거부하는 반면 "한 번 내고 끝"에는 지갑을 연다.
초기 현금흐름 확보에도 유리하다. 다만 ₩59,000 은 연 구독 2년치이므로,
**상시 판매하면 2년 이상 쓸 사용자의 장기 매출을 계속 잠식한다.**

따라서 출시 초기에만 열고 닫는다.

| 항목 | 값 |
|------|-----|
| 판매 조건 | 정식 출시일로부터 **3개월** 또는 **선착순 500명**, 먼저 도달하는 시점까지 |
| 종료 후 | 상품을 스토어에서 내리고 페이월에서 제거 |
| 기존 구매자 | 영구 유지. 종료를 이유로 회수하지 않는다 |

**종료 조건은 반드시 지킨다.** "선착순 500명" 을 표시해 놓고 계속 판매하면
전자상거래법상 기만적 표시에 해당한다. 조건을 못 지킬 것 같으면 처음부터
"출시 기념 한정" 처럼 수량을 특정하지 않는 문구를 쓴다.

구현상 종료는 RevenueCat Offering 에서 해당 패키지를 내리는 것으로 처리한다
(앱 업데이트 없이 즉시 반영). 클라이언트는 Offering 에 있는 상품만 표시하므로
별도 분기 코드가 필요 없다 — [2-2 상품 ID](#2-2-상품-id) 참고.

### 무료 / 유료 기능 매트릭스

| 기능 | 무료 | 프리미엄 |
|------|------|----------|
| 복용 일정 등록·알림·체크 | ✅ 무제한 | ✅ |
| 기본 통계 (오늘/주간 복용률) | ✅ | ✅ |
| 복용 기록 조회 | 최근 **30일** | **전체 기간** |
| **복약 리포트 PDF** | ❌ | ✅ 3/6/12개월 |
| **보호자 그룹 인원** | **1명** | **3명** |
| **미복용 알림** | 일 1회 요약 | **즉시 푸시** |
| AI 약봉투 스캔 | 5회/일 | 무제한 |
| 클라우드 백업·기기 변경 | ❌ | ✅ |
| 테마 | 포인트로 구매 | 전체 해금 |
| 영양제 가이드 | ✅ | ✅ |

### 가장 강한 유료 기능 — 복약 리포트 PDF

진료 때 의사에게 보여줄 3개월 복약 기록. **실질 효용이 가장 뚜렷하고 경쟁 앱과 차별되며
의학적 조언이 아니라 "기록 정리"이므로 규제 위험이 없다.** 결제 화면의 대표 이미지로 쓴다.

포함 항목: 기간별 복용률, 약별 순응도, 미복용 패턴(요일·시간대), 복용 시각 산포도.
**해석·권고 문구는 넣지 않는다** (아래 [주의 사항](#5-주의-사항) 참고).

---

## 2. 기술 구현

### 2-1. 결제 SDK

**RevenueCat (`react-native-purchases`)** 을 쓴다.

**Why:** iOS(StoreKit)·Android(Play Billing) 영수증 검증을 서버에서 직접 구현하지 않아도 되고,
구독 상태를 웹훅으로 받아 서버와 동기화할 수 있다. 무료 티어가 월 매출 $2,500까지 커버하므로
초기 비용이 0이다. 직접 구현 대비 최소 1~2주를 아낀다.

```
npx expo install react-native-purchases
```

`app.json` plugins 에 추가. Expo 관리형 워크플로에서 동작하며 prebuild 가 필요하다.

### 2-2. 상품 ID

App Store Connect / Play Console 양쪽에 동일 ID로 등록한다.

| 상품 ID | 유형 |
|---------|------|
| `pillarm_premium_monthly` | 자동 갱신 구독 |
| `pillarm_premium_yearly` | 자동 갱신 구독 |
| `pillarm_premium_lifetime` | 비소모성 |

RevenueCat Entitlement 는 `premium` 하나로 통일해 세 상품 모두 여기에 매핑한다.
클라이언트는 상품이 아니라 **entitlement 보유 여부만** 확인한다.

### 2-3. 서버 — 데이터 모델 변경

```prisma
enum SubscriptionTier {
  FREE
  PREMIUM
}

enum SubscriptionSource {
  APP_STORE
  PLAY_STORE
  ADMIN        // 수동 부여 (테스터·환불 대응)
}

/// 구독 상태 — RevenueCat 웹훅으로 갱신되는 서버 측 진실
model Subscription {
  id         String             @id @default(cuid())
  userId     String             @unique
  tier       SubscriptionTier   @default(FREE)
  source     SubscriptionSource?
  /// RevenueCat app_user_id (= User.id)
  productId  String?
  /// 만료 시각. 평생 이용권은 null.
  expiresAt  DateTime?
  /// 갱신 예정 여부 (해지했지만 기간이 남은 상태 구분)
  autoRenew  Boolean            @default(false)
  updatedAt  DateTime           @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([expiresAt])
}
```

`User` 모델에 관계 추가:

```prisma
subscription Subscription?
```

**`ScanUsage` 는 그대로 둔다.** 프리미엄은 한도 검사를 건너뛰기만 하면 되므로
`aiScan.ts` 의 `if (!req.user!.isAdmin && ...)` 조건에 구독 확인을 더한다.

### 2-4. 서버 API

| 메서드 | 경로 | 설명 |
|--------|------|------|
| `GET` | `/subscription` | 현재 사용자의 구독 상태 |
| `POST` | `/webhooks/revenuecat` | RevenueCat 웹훅 수신 (구매·갱신·해지·환불) |
| `POST` | `/admin/subscription` | 관리자 수동 부여/회수 |
| `GET` | `/report/adherence?months=3` | 복약 리포트 데이터 (프리미엄 전용) |

**웹훅이 구독 상태의 유일한 진실이다.** 클라이언트가 보내는 구매 완료 신호를 신뢰하지 않는다.
웹훅에는 RevenueCat Authorization 헤더 검증을 반드시 붙인다(`REVENUECAT_WEBHOOK_SECRET`).

### 2-5. 권한 검사 미들웨어

```ts
// server/src/middleware/requirePremium.ts
// 만료 시각이 지났으면 FREE 로 간주한다. 웹훅 유실에 대비한 방어.
```

**그룹 단위 프리미엄:** 보호자 그룹 소유자가 프리미엄이면 그룹 구성원 전원이 프리미엄 혜택을
받는다. 검사 순서는 `본인 구독 → 소속 그룹 소유자의 구독` 이다.

인원 상한은 **무료 1명 / 프리미엄 3명**이다. 한 명이 결제하면 가족 전체가 쓰는 구조를
유지하되, 상한을 낮춰 원가와 남용 여지를 줄인다. 상한 초과 시 초대가 거부되며,
**이미 참여 중인 구성원을 강제로 내보내지 않는다** (구독 만료로 상한이 줄어드는 경우 포함 —
기존 연결은 유지하고 신규 초대만 막는다).

### 2-6. 환경 변수 추가

```
REVENUECAT_WEBHOOK_SECRET=   # 웹훅 인증
EXPO_PUBLIC_REVENUECAT_IOS_KEY=
EXPO_PUBLIC_REVENUECAT_ANDROID_KEY=
```

---

## 3. 화면 설계

### S-PAY01: 페이월 (결제 안내)

진입 경로: 설정 → 프리미엄 / 잠긴 기능 탭 시 / 온보딩 마지막(스킵 가능)

```
┌─────────────────────────────┐
│  ← 필람 프리미엄             │
│                             │
│  [복약 리포트 PDF 미리보기]   │  ← 대표 이미지
│                             │
│  진료 때 보여드리는           │
│  3개월 복약 기록              │
│                             │
│  ✓ 복약 리포트 PDF           │
│  ✓ 보호자 3명까지 관리        │
│  ✓ 미복용 즉시 알림           │
│  ✓ 무제한 약봉투 스캔         │
│  ✓ 클라우드 백업             │
│                             │
│  ┌───────────────────────┐  │
│  │ 연간  ₩29,000  38%↓   │  │  ← 기본 선택
│  │ 월 ₩2,417 꼴          │  │
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │ 월간  ₩3,900          │  │
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │ 평생  ₩59,000  1회 결제│  │  ← 한정 기간에만 노출
│  │ 출시 기념 한정          │  │
│  └───────────────────────┘  │
│                             │
│  [      구독하기       ]     │
│                             │
│  구매 복원 · 이용약관         │
│                             │
│  알림·복용 체크 등 핵심 기능은 │
│  프리미엄 없이도 계속         │
│  무료로 사용하실 수 있습니다.  │
└─────────────────────────────┘
```

**마지막 문구는 반드시 넣는다.** 심사 대응이자 신뢰 요소다.

### S-PAY02: 결제 완료

축하 문구 + "보호자 초대하러 가기" CTA. 그룹 기능으로 유도해 유지율을 높인다.

### S-REPORT01: 복약 리포트

기간 선택(3/6/12개월) → 미리보기 → PDF 저장·공유.
`expo-print` + `expo-sharing` 으로 생성한다. 서버 렌더링 불필요.

### 잠금 UI 원칙

잠긴 기능은 **숨기지 않고 자물쇠 아이콘과 함께 보여준다.** 무엇을 얻는지 알아야 결제한다.
단, 홈 화면의 일상 동선(복용 체크)에는 어떤 잠금 표시도 넣지 않는다.

---

## 4. 구현 순서

| 순서 | 작업 | 예상 소요 |
|------|------|-----------|
| 1 | 스토어 상품 등록 (App Store Connect / Play Console) + 세금·계약 정보 | 0.5일 |
| 2 | RevenueCat 프로젝트 설정 + entitlement 매핑 | 0.5일 |
| 3 | 서버: `Subscription` 모델 + 마이그레이션 | 0.5일 |
| 4 | 서버: `/webhooks/revenuecat` + 서명 검증 + 상태 동기화 | 1일 |
| 5 | 서버: `requirePremium` 미들웨어 + 그룹 상속 로직 | 0.5일 |
| 6 | 서버: 기존 라우트에 권한 적용 (스캔 한도·보호자 인원·기록 기간) | 0.5일 |
| 7 | 클라이언트: `react-native-purchases` 연동 + `subscriptionStore` | 1일 |
| 8 | `PaywallScreen` (S-PAY01) | 1일 |
| 9 | 복약 리포트 PDF (S-REPORT01) | 1.5일 |
| 10 | 잠금 UI 적용 (기록·보호자·스캔·테마) | 1일 |
| 11 | 샌드박스 결제 테스트 (iOS/Android 각각, 구매·복원·해지·환불) | 1.5일 |

**총 예상: 약 9.5일**

---

## 5. 완료 기준 (AC)

- [ ] 무료 사용자도 일정 등록·알림·복용 체크·기본 통계를 **제한 없이** 사용할 수 있다
- [ ] 세 가지 상품(월/연/평생) 모두 iOS·Android 에서 구매된다
- [ ] "구매 복원" 으로 기기 변경·재설치 후 구독이 복원된다
- [ ] 구독 상태는 **서버 웹훅 기준**이며, 클라이언트 구매 신호만으로 권한이 열리지 않는다
- [ ] 웹훅이 유실되어도 `expiresAt` 만료 검사로 권한이 자동 회수된다
- [ ] 보호자 그룹 소유자가 프리미엄이면 구성원 전원이 프리미엄 혜택을 받는다
- [ ] 보호자 인원 상한(무료 1명 / 프리미엄 3명)을 초과하면 신규 초대가 거부된다
- [ ] 구독 만료로 상한이 줄어도 **기존 구성원이 강제로 해제되지 않는다**
- [ ] 평생 이용권은 RevenueCat Offering 에서 내리면 앱 업데이트 없이 페이월에서 사라진다
- [ ] 해지 후에도 잔여 기간까지는 프리미엄이 유지된다
- [ ] 환불 시 웹훅으로 권한이 즉시 회수된다
- [ ] 오프라인 상태에서 마지막으로 확인된 구독 상태로 동작한다 (오프라인 우선 원칙)
- [ ] 복약 리포트 PDF 가 3/6/12개월 기간으로 생성·공유된다
- [ ] 페이월에 "핵심 기능은 무료" 문구가 표시된다
- [ ] 관리자가 수동으로 구독을 부여·회수할 수 있다

---

## 6. 주의 사항

### 하지 않기로 한 것

**광고 — 넣지 않는다.**
복약 데이터는 개인정보보호법상 **민감정보**다. 프로파일링 기반 광고를 붙이면 별도 명시 동의가
필요하고 법적 노출이 크다. 고령 사용자는 광고 단가도 낮고 오조작 위험도 크다.
얻는 것보다 잃는 것이 많다.

**영양제 제휴·커머스 — 하지 않는다.**
건강기능식품 광고는 사전심의 대상이다. 무엇보다 약 알림 앱이 영양제를 판매하면
**앱의 신뢰 전제 자체가 무너진다.** `supplementGuide` 는 정보 제공에 머무른다.

### 규제·심사

- **의료법·약사법**: 리포트에 해석이나 권고를 넣지 않는다. "복용률이 낮으니 ○○하세요" 같은
  문구는 의료 조언으로 해석될 수 있다. **기록의 정리·시각화까지만** 한다.
- **전자상거래법**: 구독 해지 방법을 앱 내에서 명확히 안내해야 한다. 스토어 구독 관리 화면
  딥링크를 설정에 배치한다. **평생 이용권의 "한정" 표시는 실제 종료 조건을 지켜야 한다** —
  수량·기간을 명시해 놓고 계속 판매하면 기만적 표시에 해당한다.
- **애플 심사**: 디지털 콘텐츠는 IAP 필수. 외부 결제 링크를 넣으면 반려된다.
- **미성년자 결제**: 스토어 결제를 쓰므로 별도 처리는 불필요하나, 약관에 명시한다.

### 기술

- **오프라인 우선 원칙 유지**: 구독 상태를 확인하지 못해도 앱은 정상 동작해야 한다.
  마지막으로 확인된 상태를 로컬에 캐시하고, 확인 실패 시 **기존 권한을 유지**한다
  (권한을 뺏는 방향으로 실패하지 않는다).
- **가격 변경 대비**: 가격을 코드에 하드코딩하지 않는다. RevenueCat Offering 에서 받아 표시한다.
- **기존 사용자**: 정식 출시 이전 가입자에게는 평생 프리미엄을 부여하는 것을 검토한다
  (`SubscriptionSource.ADMIN`). 초기 사용자 이탈 방지와 리뷰 확보에 유리하다.

---

## 7. 이후 — B2B (Phase 6 후보)

개인 구독보다 **B2B 시장이 훨씬 크다.** 개인 구독은 그 길로 가기 위한 검증 단계로 본다.

**착수 조건:** 개인 구독으로 유료 전환율과 리텐션을 검증한 이후.

### 7-1. 우선순위

세 대상 중 **방문요양기관이 1순위**다. 결제 결정권자와 사용자가 같은 조직 안에 있고
(요양보호사 → 센터장), 복약 관리가 이미 그들의 업무이며, 앱이 그 업무를 대체하는 것이
아니라 기록을 자동화해 주기 때문에 도입 저항이 가장 낮다.

| 순위 | 대상 | 도입 난이도 | 계약 규모 |
|------|------|------------|-----------|
| 1 | **방문요양기관 (재가장기요양)** | 낮음 | 중 |
| 2 | 약국 | 중 | 소 |
| 3 | 임상시험 사이트 (CRO·병원) | 높음 | 대 |

### 7-2. 방문요양기관

**문제:** 요양보호사가 방문할 때마다 어르신 복약 상태를 종이 기록지에 적고,
센터로 돌아와 다시 옮겨 적는다. 누락과 이중 입력이 상시 발생한다.

| 필요 기능 | 기존 자산 |
|-----------|-----------|
| 요양보호사 1명 → 담당 어르신 N명 대시보드 | `CareMonitorScreen` 확장 |
| 센터 관리자 뷰 (전체 요양보호사·수급자 현황) | 신규 (웹) |
| 복약 체크 위임 (보호자가 아닌 직원이 대신 체크) | `CareMember` 역할 확장 |
| 월간 복약 리포트 일괄 출력 | Feature 1 의 PDF 리포트 재사용 |
| 기관 계정 관리 (직원 입퇴사 처리) | 신규 |

**과금:** 수급자당 월 ₩2,000 (최소 10명부터). 30명 센터 기준 월 ₩60,000.
기관 정액제보다 수급자당 과금이 낫다 — 센터 규모 편차가 크고, 확장에 따라 매출이 따라 커진다.

### 7-3. 약국

**문제:** 복약지도를 해도 환자가 집에 가면 잊는다. 재방문·복약 순응도를 확인할 방법이 없다.

| 필요 기능 | 기존 자산 |
|-----------|-----------|
| 약국 QR 발급 → 환자가 스캔하면 처방 일정 자동 등록 | 스캔·초대 코드 로직 재사용 |
| 조제 내역 → 일정 자동 변환 | Phase 4 스캔 파이프라인 |
| 복약 종료 시점 재방문 알림 | 알림 스케줄러 확장 |

**과금:** 약국당 월 ₩30,000 정액. 약국은 규모 편차가 작아 정액이 맞다.

**주의:** 약국이 환자 복약 데이터를 조회하는 구조는 **환자의 명시적 동의가 필수**이며,
조회 범위를 해당 약국이 조제한 약으로 한정해야 한다. 전체 복약 이력을 약국에 열지 않는다.

### 7-4. 임상시험 사이트

**문제:** 복약 순응도(compliance)를 피험자 자가보고에 의존해 신뢰도가 낮다.

| 필요 기능 | 난이도 |
|-----------|--------|
| 익명화된 순응도 데이터 내보내기 | 중 |
| 감사 추적 (누가 언제 무엇을 수정했는가) | 높음 — 기존 스키마에 이력 테이블 필요 |
| 데이터 무결성 요건 (21 CFR Part 11 유사) | 높음 |

**과금:** 시험당 계약 (수천만 원 규모). 대신 요구 수준이 완전히 다르다.

**이 시장은 규제 요구가 커서 별도 제품에 가깝다.** 1·2순위를 검증한 뒤에 판단한다.

### 7-5. 공통 선행 과제

- **웹 대시보드**: 기관 사용자는 모바일이 아니라 PC 로 일한다. B2B 는 웹 없이 성립하지 않는다.
- **개인정보 처리 위탁 계약**: 기관이 수급자 건강정보를 조회하므로 위탁 계약과
  수급자 동의 절차가 선행되어야 한다. 개인 대상 서비스와 법적 구조가 다르다.
- **의료기기 규제 확인**: 복약 기록·알림에 머무르면 해당 없으나, 순응도를 근거로
  **판단이나 권고를 제공하는 순간** 디지털 의료기기 해당 여부를 검토해야 한다.
  Feature 1 의 "해석·권고를 넣지 않는다" 원칙이 B2B 에서도 그대로 적용된다.
