/**
 * 서비스 사용량 한도 — 라우트와 통계 양쪽에서 참조한다.
 *
 * 한도 값을 라우트 안에 두면 `/admin/stats` 가 "한도에 도달한 사용자 수" 를
 * 집계할 때 숫자가 중복 정의된다. 한 곳에서만 바꾸도록 여기로 모은다.
 */

/** 약봉투 스캔(AI Vision) 1인 1일 호출 상한. 관리자는 예외. */
export const DAILY_SCAN_LIMIT = 5;
